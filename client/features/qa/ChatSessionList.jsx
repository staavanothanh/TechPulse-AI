import { safeDate } from './qa-validation.js'

export default function ChatSessionList({ sessions = [], selectedId, loading = false, onSelect, onDelete, onClear, onLoadMore, hasNext = false, statusRef, emptyHeadingRef, statusText = 'Trạng thái danh sách phiên', deleteCooldown = 0, clearCooldown = 0, selectionLocked = false }) {
  return <aside className="qa-session-rail" aria-label="Lịch sử phiên"><div className="qa-rail-header"><h2>Lịch sử phiên</h2><button className="qa-icon-button" type="button" aria-label="Xóa tất cả phiên" onClick={onClear} disabled={clearCooldown > 0}>×</button></div><p className="sr-only" ref={statusRef} tabIndex="-1">{statusText}</p>{loading ? <p className="qa-muted" aria-busy="true">Đang tải phiên…</p> : null}<div className="qa-session-list">{!loading && sessions.length === 0 ? <p className="qa-empty" ref={emptyHeadingRef} tabIndex="-1">Chưa có phiên hỏi đáp.</p> : sessions.map((session) => <div className="qa-session-item" key={session.id} aria-current={selectedId === session.id ? 'true' : undefined}><button className="qa-session-select" type="button" onClick={() => onSelect?.(session.id)} disabled={selectionLocked}><strong>{session.title || 'Phiên hỏi đáp'}</strong><small>{safeDate(session.updatedAt)}</small></button><button className="qa-icon-button" type="button" aria-label={`Xóa ${session.title || 'phiên hỏi đáp'}`} onClick={() => onDelete?.(session.id)} disabled={deleteCooldown > 0}>×</button></div>)}</div>{hasNext ? <button className="qa-button qa-load-more" type="button" onClick={onLoadMore}>Tải thêm phiên</button> : null}</aside>
}
